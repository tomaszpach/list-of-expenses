import React from 'react';

const InputBtn = ({value}) => {
    return (
        <input
            value={value}
            className="btn"
            type="submit"
        />
    );
};

export default InputBtn;
